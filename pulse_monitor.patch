--- a/scripts/pulse_monitor.py
+++ b/scripts/pulse_monitor.py
@@ -1,10 +1,13 @@
 import tkinter as tk
 from tkinter import ttk, scrolledtext
 import serial
 import serial.tools.list_ports
 import threading
 import queue
-import time
 import re
 
+/* Similarity threshold for anomaly detection (must match firmware) */
+SIMILARITY_NORMAL_THRESHOLD = 90  # >= 90% is normal operation
+
 class PulseMonitorApp:
     def __init__(self, root):
         self.root = root
@@ -242,7 +245,7 @@ class PulseMonitorApp:
             self.similarity_label.config(text=f"Similarity: {self.last_similarity}%")
             
             if self.ml_state == "INFERENCING":
-                if self.last_similarity >= 90:
+                if self.last_similarity >= SIMILARITY_NORMAL_THRESHOLD:
                     self.update_status("✓ NORMAL", "#27ae60")
                 else:
                     self.update_status("⚠ ANOMALY DETECTED!", "#e74c3c")
@@ -288,7 +291,7 @@ class PulseMonitorApp:
             self.update_log("--- Verbose logs DISABLED ---")
     
     def poll_status(self):
-        """Automatically poll device status every 250ms"""
+        """Automatically poll device status every 1000ms"""
         if self.status_polling_active and self.is_connected:
             self.send_command("STATUS")
             # Schedule next poll
-            self.root.after(250, self.poll_status)
+            self.root.after(1000, self.poll_status)
 
     def on_closing(self):
